# A module is simply a python file that contains code that can be re-used in other files. Modules allow us to break up complex programs into smaller, more manageable pieces across mutiple files.
# Built-in, Custom, 3rd Party
# Standard library -> python's built-in modules

import random 
print(random.randint(1,6))