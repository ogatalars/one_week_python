age = input('How old are you? ')
# The input function will prompt a user to enter some input, converts it into a STRING, and then returns it. We can use it to gather information from the user.
first_name = str(input("What's your first name? "))
print("Hello There! " + first_name)