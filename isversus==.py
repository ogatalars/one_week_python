a = [1,2,3]
b = [1,2,3]
print(a == b) # True
print(a is b) # False

#  USE == to compare the contents inside two lists. Do they hold the same values?
#  use IS to compare the references of two lists. Do they point to the same object?