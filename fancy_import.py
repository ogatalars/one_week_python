import random as randão
print(randão.randint(1,5))
from math import pi # só import o pi
print(pi)


# Use the as keyword to import a module and give it a custom name in your script; this is useful when you want to use the same module in multiple files.